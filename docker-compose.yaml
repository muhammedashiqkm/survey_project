version: '3.8'

services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    
    logging:
      driver: "json-file"
      options:
        max-size: "10m"  
        max-file: "3"    